const prefix = 'blog'
const suffix = 'key'
export const STORE_KEY = `${prefix}-storeinfo-${suffix}`

export const USERINFO_KEY = `${prefix}-userinfo-${suffix}`

export const FAVORITE_KEY = `${prefix}-favorite-${suffix}`

export const CURRENTBLOG_KEY = `${prefix}-currentblog-${suffix}`
